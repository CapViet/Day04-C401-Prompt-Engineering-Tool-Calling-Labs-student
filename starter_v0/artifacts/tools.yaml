# starter_v0 — tool declarations (intentionally vague).
# Names and descriptions here are a rough first draft. Routing rules, name->handle
# mapping, arg conventions (news/timeframe/Top), and the send-confirmation boundary
# are NOT spelled out anywhere yet. Your job is to make these declarations + the
# system prompt good enough that the agent routes correctly on the eval.
#
# If you rename a tool, keep the name in sync across:
#   this file  ->  tools/__init__.py (TOOL_FUNCTIONS)  ->  data/eval_base.json + data/eval_research_extension.json
tools:
  # ---------------- Core tools ----------------
  - name: clarify
    description: "Hỏi lại user khi thiếu thông tin bắt buộc (handle Twitter, URL) hoặc cần xác nhận trước khi thực hiện hành động ghi/gửi. KHÔNG đoán khi thiếu thông tin."
    parameters:
      type: object
      properties:
        question: {type: string, default: "", description: "Câu hỏi"}
        response_type: {type: string, enum: [text, yes_no, choice], default: "text", description: "Kiểu trả lời"}
        options: {type: array, default: [], items: {type: string}, description: "Các lựa chọn"}
      required: [question]

  - name: timeline
    description: "Lấy tweet/post gần đây của MỘT tài khoản Twitter/X cụ thể qua screenname. Dùng khi user hỏi tweet của một người cụ thể (vd: 'tweet của Elon Musk'). KHÔNG dùng cho tìm kiếm theo chủ đề."
    parameters:
      type: object
      properties:
        screenname: {type: string, default: "", description: "Tên tài khoản"}
        limit: {type: integer, default: 5, description: "Số lượng"}
      required: [screenname]

  - name: social_search
    description: "Tìm tweet/post theo CHỦ ĐỀ hoặc từ khóa trên Twitter/X. Dùng khi user hỏi 'mọi người đang nói gì về X' — không phải tweet của một người cụ thể. search_type=Top cho 'phổ biến/top', Latest cho mặc định."
    parameters:
      type: object
      properties:
        query: {type: string, default: "", description: "Từ khóa"}
        search_type: {type: string, enum: [Latest, Top], default: "Latest", description: "Cách sắp xếp"}
        limit: {type: integer, default: 5, description: "Số lượng"}
      required: [query]

  - name: lookup
    description: "Tìm kiếm thông tin trên web. Dùng topic=news cho tin tức/thời sự, topic=general cho thông tin chung. Dùng timeframe=day cho 'hôm nay', week cho 'tuần này', month/year tương tự."
    parameters:
      type: object
      properties:
        query: {type: string, default: "", description: "Truy vấn"}
        topic: {type: string, enum: [general, news], default: "general", description: "Phân loại"}
        timeframe: {type: string, enum: [day, week, month, year], default: "week", description: "Khoảng thời gian"}
        max_results: {type: integer, default: 5, description: "Số kết quả"}
      required: [query]

  - name: crawl
    description: "Crawl nhiều trang của một website bắt đầu từ URL gốc. Dùng khi user muốn đọc toàn bộ site hoặc một section (vd: tất cả bài blog), không chỉ một trang đơn. Nếu chỉ cần một URL cụ thể, dùng fetch thay thế."
    parameters:
      type: object
      properties:
        url: {type: string, default: "", description: "URL gốc để bắt đầu crawl"}
        limit: {type: integer, default: 5, description: "Số trang tối đa"}
        max_chars_per_page: {type: integer, default: 2000, description: "Số ký tự tối đa mỗi trang"}
      required: [url]

  - name: fetch
    description: "Đọc nội dung của MỘT URL cụ thể đã được user cung cấp. KHÔNG đoán URL. Nếu chưa có URL, dùng clarify để hỏi."
    parameters:
      type: object
      properties:
        url: {type: string, default: "", description: "Địa chỉ"}
      required: [url]

  - name: format
    description: "Trình bày dữ liệu đã có thành văn bản."
    parameters:
      type: object
      properties:
        items:
          type: array
          default: []
          description: "Danh sách item"
          items:
            type: object
            properties:
              title: {type: string}
              url: {type: string}
              source: {type: string}
              summary: {type: string}
              section: {type: string}
        template: {type: string, enum: [brief, sections, bullets, thread, daily_ai_vn], default: "sections", description: "Khuôn"}
        headline: {type: string, default: "", description: "Tiêu đề"}
      required: [items, template]

  # ---------------- Bonus tools ----------------
  - name: send
    description: "Gửi text lên Telegram. LUÔN gọi clarify(response_type=yes_no) để xác nhận trước khi gọi tool này. Không bao giờ tự gửi mà không có xác nhận."
    parameters:
      type: object
      properties:
        text: {type: string, default: "", description: "Nội dung"}
        confirmed: {type: boolean, default: false, description: "Cờ xác nhận"}
      required: [text]

  - name: policy
    description: "Tìm trong tài liệu nội bộ."
    parameters:
      type: object
      properties:
        query: {type: string, default: "", description: "Truy vấn"}
        policy_area: {type: string, enum: [all, ai_research, source_citation, data_privacy, external_publishing, tool_usage], default: "all", description: "Nhóm"}
        top_k: {type: integer, default: 3, description: "Số mục"}
      required: [query]

  - name: papers
    description: "Tìm bài báo khoa học."
    parameters:
      type: object
      properties:
        query: {type: string, default: "", description: "Từ khóa"}
        max_results: {type: integer, default: 5, description: "Số lượng"}
        sort_by: {type: string, enum: [relevance, lastUpdatedDate, submittedDate], default: "relevance", description: "Sắp xếp"}
      required: [query]

  - name: paper_text
    description: "Lấy nội dung text của một bài báo."
    parameters:
      type: object
      properties:
        arxiv_url: {type: string, default: "", description: "ID hoặc địa chỉ"}
        max_pages: {type: integer, default: 5, description: "Số trang"}
        max_chars: {type: integer, default: 8000, description: "Số ký tự"}
      required: [arxiv_url]
